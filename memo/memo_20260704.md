- 実行前確認の結果、必要パッケージは未導入で、プロジェクト内に仮想環境もない。
- 実行にはファイル作成とネットワーク通信が必要。


# 
プロジェクト直下で順番に実行。
```bash
 % python3 -m venv .venv
 % source .venv/bin/activate
 % python -m pip install --upgrade pip
 % python -m pip install torch transformers nltk tqdm tabulate sentencepiece
```

# 
ファイルを作成せずに最小例を実行。

```bash
PYTHONPATH="$PWD/src" python -c 'import factual_scene_graph; print(factual_scene_graph.__file__)'
```

```bash
PYTHONPATH=src python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; print("Loading model..."); parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); print("Parsing..."); result=parser.parse(["2 beautiful pigs are flying on the sky with 2 bags on their backs"], beam_size=1, return_text=True); print("Result:"); print(result[0])'
```
そのまま1行全体を貼り付けてEnter。
初回はモデルのダウンロードが始まる。

- 処理内容は README.md の「Quick Start → Minimal example」に記載されている。
- ただし、上記1行コマンドはREADMEのコードをターミナルで実行しやすいように変形したもの。解析処理自体はREADMEの最小例と同じ。
- 変更点は次のとおり。
  - PYTHONPATH=src：未インストールのローカルソースを読み込むため追加
  - python -c '...'：heredocを避けるため1行化
  - print("Loading model...") など：進行状況確認用に追加
  - モデルID、入力文、beam_size=1、return_text=True：README記載どおり

  
# transformersバージョン確認
```bash
(.venv) t@Mac FactualSceneGraph %  python --version
Python 3.14.0
(.venv) t@Mac FactualSceneGraph % python -c 'import transformers; print(transformers.__version__)'
5.13.0
(.venv) t@Mac FactualSceneGraph % 
```

transformers を4系へ固定する。
```bash
python -m pip install --force-reinstall "transformers==4.46.3" sentencepiece
```

バージョンの確認
```bash
python -c 'import transformers; print(transformers.__version__)'
```

# Pythonバージョンの固定
```bash
  deactivate
  /usr/local/bin/python3.11 --version
  /usr/local/bin/python3.11 -m venv venv
  source venv/bin/activate
```

# 最小依存関係を導入
```bash
python -m pip install --upgrade pip
python -m pip install torch "transformers==4.46.3" nltk tqdm tabulate sentencepiece
```

# 確認
```bash
  python --version
  python -c 'import transformers; print(transformers.__version__)'
```
(venv) t@Mac FactualSceneGraph % python --version
Python 3.11.6
(venv) t@Mac FactualSceneGraph % python -c 'import transformers; print(transformers.__version__)'
4.46.3

# 解析コマンド
```bash
PYTHONPATH=src python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; print("Loading model..."); parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); print("Parsing..."); result=parser.parse(["2 beautiful pigs are flying on the sky with 2 bags on their backs"], beam_size=1, return_text=True); print("Result:"); print(result[0])'
```

# 実行結果
```bash
(venv) t@Mac FactualSceneGraph % PYTHONPATH=src python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; print("Loading model..."); parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); print("Parsing..."); result=parser.parse(["2 beautiful pigs are flying on the sky with 2 bags on their backs"], beam_size=1, return_text=True); print("Result:"); print(result[0])'
Loading model...
Parsing...
  0%|                                    | 0/1 [00:00<?, ?it/s]/Users/t/ObsidianVault/JAIST/masters-research/FactualSceneGraph/venv/lib/python3.11/site-packages/transformers/tokenization_utils_base.py:4114: UserWarning: `as_target_tokenizer` is deprecated and will be removed in v5 of Transformers. You can tokenize your labels by using the argument `text_target` of the regular `__call__` method (either in the same call as your input texts if you use the same keyword arguments, or in a separate call.
  warnings.warn(
100%|████████████████████████████| 1/1 [00:03<00:00,  3.11s/it]
Result:
( bags , is , 2 ) , ( bags , on back of , pigs ) , ( pigs , fly on , sky ) , ( pigs , is , 2 ) , ( pigs , is , beautiful )
(venv) t@Mac FactualSceneGraph % 
```

- ↑動作確認は成功。
  - モデル読込：成功
  - CPU推論：成功（約3.11秒）
  - Scene Graph生成：成功
  - 出力：READMEの期待例とほぼ一致

- as_target_tokenizer は非推奨警告だが、現在の実行には影響ない。
- transformers 4.46.3 では動作し、将来のv5ではコード修正が必要。
- 今回の再現条件は以下です。
  - Python 3.11
  - transformers 4.46.3
  - device=cpu
  - model=lizhuang144/flan-t5-base-VG-factual-sg

- ここまでで、最小構成の動作確認完了。
- 続いて、日本語または災害報告文を入力した場合の出力確認。


# 日本語の災害報告文を入力した場合の出力確認
- まず短い1文で確認する。
  - 例）作業員が足場から転落した。
- 確認観点
  - 作業員、足場などの物体が抽出されるか
  - 転落という関係・動作が表現されるか
  - 日本語が英語へ変換されるか
  - 無関係な物体や関係が生成されないか
  - 空出力や壊れた三つ組にならないか

```bash
PYTHONPATH="$PWD/src" python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); text="作業員が足場から転落した。"; print("Input:", text); result=parser.parse([text], beam_size=1, return_text=True); print("Result:"); print(result[0])'
```

# 実行結果
```bash
(venv) t@Mac FactualSceneGraph % PYTHONPATH="$PWD/src" python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); text="作業員が足場から転落した。"; print("Input:", text); result=parser.parse([text], beam_size=1, return_text=True); print("Result:"); print(result[0])'
Input: 作業員が足場から転落した。
  0%|                                    | 0/1 [00:00<?, ?it/s]/Users/t/ObsidianVault/JAIST/masters-research/FactualSceneGraph/venv/lib/python3.11/site-packages/transformers/tokenization_utils_base.py:4114: UserWarning: `as_target_tokenizer` is deprecated and will be removed in v5 of Transformers. You can tokenize your labels by using the argument `text_target` of the regular `__call__` method (either in the same call as your input texts if you use the same keyword arguments, or in a separate call.
  warnings.warn(
100%|████████████████████████████| 1/1 [00:02<00:00,  2.78s/it]
Result:
( giraffe , is , eating ) , ( giraffe , is , standing ) , ( giraffe , is , tall )
(venv) t@Mac FactualSceneGraph % 
```

# 結果の考察
- 日本語直接入力は失敗。
- 入力に存在しない giraffe が生成されており、完全な幻覚です。
  - 作業員：未抽出
  - 足場：未抽出
  - 転落：未抽出
  - キリンの属性・動作：根拠なく生成


# 英訳して比較してみる。
  - 例）A worker fell from the scaffolding.（日本語訳：作業員が足場から転落した。）
```bash
PYTHONPATH="$PWD/src" python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); text="A worker fell from the scaffolding."; print("Input:", text); result=parser.parse([text], beam_size=1, return_text=True); print("Result:"); print(result[0])'
```

# 実行結果
```bash
(venv) t@Mac FactualSceneGraph % PYTHONPATH="$PWD/src" python -c 'from factual_scene_graph.parser.scene_graph_parser import SceneGraphParser; parser=SceneGraphParser("lizhuang144/flan-t5-base-VG-factual-sg", device="cpu"); text="A worker fell from the scaffolding."; print("Input:", text); result=parser.parse([text], beam_size=1, return_text=True); print("Result:"); print(result[0])'
Input: A worker fell from the scaffolding.
  0%|                                    | 0/1 [00:00<?, ?it/s]/Users/t/ObsidianVault/JAIST/masters-research/FactualSceneGraph/venv/lib/python3.11/site-packages/transformers/tokenization_utils_base.py:4114: UserWarning: `as_target_tokenizer` is deprecated and will be removed in v5 of Transformers. You can tokenize your labels by using the argument `text_target` of the regular `__call__` method (either in the same call as your input texts if you use the same keyword arguments, or in a separate call.
  warnings.warn(
100%|████████████████████████████| 1/1 [00:02<00:00,  2.40s/it]
Result:
( worker , fall from , scaffolding )
(venv) t@Mac FactualSceneGraph % 
```

# 結果の考察
- 英訳入力では適切に解析できている。

  ( worker , fall from , scaffolding )

- 評価：
  - 主体：worker — 正しい
  - 関係・動作：fall from — 正しい
  - 対象・起点：scaffolding — 正しい
  - 入力にない情報の幻覚：なし

- 今回の比較から、現実的な処理経路は次の形。
  - 日本語災害報告文
    - → 英訳
    - → FACTUALによるScene Graph抽出
    - → 原文との整合性確認

- ただし、この結果は単純な1文・1関係での成功例にすぎない。
- 複数文、状態変化、時間順序、同一物体の継続性はまだ検証されていない。
- また、FACTUALの通常の三つ組だけでは、シーン順序や状態変化を明示的に保持できないと考えられる。
- これは研究の中間表現で補うべき部分となるはずである。



```bash

```


```bash

```


```bash

```


```bash

```


```bash

```


```bash

```


```bash

```



  1. 対象
      - FactualSceneGraph/.venv/ を新規作成
      - ソースコードは変更しない
      - モデルキャッシュは /private/tmp/factual-hf-cache に保存

  2. 実施内容
      - Python 3.11の仮想環境を作成
      - setup.py に基づいて依存関係をインストール
      - READMEの最小例をCPUまたはApple SiliconのMPSで実行

  3. 影響
      - .venv/ に多数の依存ファイルが作成される
      - Hugging Faceから学習済みモデルがダウンロードされる

  4. リスク
      - 数GB規模のディスク使用とダウンロード時間
      - 最新依存パッケージとの互換性問題
      - READMEとテストでモデルIDが異なるため、まずREADME記載のモデルを使用

  5. 検証方法
      - パッケージのimport確認
      - READMEの英文1件を解析
      - Scene Graph出力とエラー、実行時間、使用デバイスを記録
      - git status でソース変更がないことを確認

  この方針で .venv の作成、依存関係とモデルのダウンロード、最小例の実行を進めてよいですか？